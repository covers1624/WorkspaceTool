rootProject.name = 'WorkspaceTool'

include 'common'
include 'core'
include 'forge'
include 'gradle'
include 'intellij'
include 'minecraft'
include 'wrapper'

rootProject.children.each {
    it.projectDir = file("subprojects/${it.name}")
}
