java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
        vendor = JvmVendorSpec.ADOPTIUM
    }
}

archivesBaseName = 'gradle-test-api'

dependencies {
    api project(":api")
    api project(":gradle")

    api gradlelibs.tooling
}
